en:
  site_settings:
    aws_push_enabled: "Check to enable AWS Push Notifications for iOS"
    aws_app_id: "App ID"
    aws_rest_api_key: "REST API Key"
de:
  site_settings:
    aws_push_enabled: "Für AWS Push Notifications nach iOS aktivieren"
    aws_app_id: "App ID"
    aws_rest_api_key: "REST API Key"
